
 //Example Input: ['brickwork', 'brickwork', 'brickwork', 'cement']
        // Example Output:  [['bob@brickwork.com', 'alice@example.com'], ['bob@brickwork.com']]

let count =0;
let numworkers = 3;

public void init() {
        scheduler = new WorkScheduler(Arrays.asList(
                new Worker(
                        "alice@example.com",
                        Set.of("brickwork", "drywall"),
                        100),
                new Worker(
                        "bob@brickwork.com",
                        Set.of("brickwork"),
                        90),
                new Worker(
                        "charlie@cement.com",
                        Set.of("cement"),

for(input){
    //

}

let res = {brickwork: 3, cement:1};

let setres = [brickwork, cement];

let obj ={};
for(setres){
    obj[setres] = [];
    let count =0;
    for(workers){
        if(workers.trare.containes(setres[i]) && count <= res[brickwork] ){
            obj[setres].add(email);
            count++;
        }
    }
}

obj = {
    brickwork: [alice@example.com,bob@brickwork.com],
    cement: [charlie@cement.com]
}
obj = {
    brickwork: {count:2,workers:[alice@example.com,bob@brickwork.com]},
    cement: {count:1,workers:[charlie@cement.com]}
}

let semioutput = [[alice@example.com,bob@brickwork.com, charlie@cement.com], []]
let out =[];
function helper(currentday){
    let mk = [];
    if(something) return;
    for(let i=0; obj.length; ){
        
    }

    if(true){
        out.push(currentday);
    }

    helper([])
}

helper([])

let secondarr = [];
for(setres){
    let suffworkers = obj[setres[i]].length;
    let needworkers = res[setres[i]];
    let diff = needworkers - suffworkers;
    if(needworkers > suffworkers){
        while(diff > 0){
            let nextdayworkers = suffworkers - diff
                //diff is 1

                let val = obj[setres].splice(diff);
                secondarr.push(val);
            
        }
    }

}

semioutput.add(secondarr);

semioutput = [[alice@example.com,bob@brickwork.com, charlie@cement.com], [alice@example.com]];


